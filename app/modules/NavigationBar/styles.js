import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  topBar: {
    backgroundColor: '#CCC',
    flexDirection: 'row',
    alignItems: 'center'
  },
  topBarTitle: {
    fontSize: 20,
    fontWeight: '500',
    margin: 5,
    flex: 1
  }
});
